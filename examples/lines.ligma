;random code for testing

;" i need my begonis! " print
;isit (lambda 100 > if ("true" print) else ("false" print)) set


;TODO: find out why this trashes the stack. would be good to put in the standard library
;foreach (lambda over if (over car over execute) else (return) over cdr over recurse) set ;figure o


; 2 3

; 3


